// Optional render-only registrations for Nightfire blocks. Import this module
// explicitly when registry-driven rendering is needed. It never imports editor
// components.

import "./markup/render";
